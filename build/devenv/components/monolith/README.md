# monolith

**Deprecated**: This package exists solely to break a circular import during the
incremental extraction of the phased component runtime from the legacy monolith.

Do not add new functionality here. Once all consumers have migrated away from
the root `build/devenv` package, this package will be removed.
