package bootstrap

const (
	// DefaultECDSASigningKeyName is the default name for the ECDSA signing key.
	// It is recommended that the default is always used to ensure less configuration overhead.
	DefaultECDSASigningKeyName = "bootstrap_default_ecdsa_signing_key"

	// DefaultEdDSASigningKeyName is the default name for the EdDSA signing key.
	// It is recommended that the default is always used to ensure less configuration overhead.
	DefaultEdDSASigningKeyName = "bootstrap_default_eddsa_signing_key"

	// DefaultCSAKeyName is the default name for the CSA key.
	// It is recommended that the default is always used to ensure less configuration overhead.
	DefaultCSAKeyName = "bootstrap_default_csa_key"
)
