overlay: 1.0.0
info:
  title: Overlay
  version: 0.0.0
actions:
  - target: "$.paths['/v1/messages'].get.parameters[?(@.name=='sourceChainSelectors')].schema.items"
    update:
      x-go-type: protocol.ChainSelector
      x-go-type-import:
        path: github.com/smartcontractkit/chainlink-ccv/protocol

  - target: "$.paths['/v1/messages'].get.parameters[?(@.name=='destChainSelectors')].schema.items"
    update:
      x-go-type: protocol.ChainSelector
      x-go-type-import:
        path: github.com/smartcontractkit/chainlink-ccv/protocol

  - target: "$.paths['/v1/verifierresult'].get.parameters[?(@.name=='sourceChainSelectors')].schema.items"
    update:
      x-go-type: protocol.ChainSelector
      x-go-type-import:
        path: github.com/smartcontractkit/chainlink-ccv/protocol

  - target: "$.paths['/v1/verifierresult'].get.parameters[?(@.name=='destChainSelectors')].schema.items"
    update:
      x-go-type: protocol.ChainSelector
      x-go-type-import:
        path: github.com/smartcontractkit/chainlink-ccv/protocol
