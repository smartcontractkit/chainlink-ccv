// Package policyapi provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.5.1 DO NOT EDIT.
package policyapi

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"net/url"
	"strings"
)

const (
	HmacApiKeyScopes    = "HmacApiKey.Scopes"
	HmacSignatureScopes = "HmacSignature.Scopes"
	HmacTimestampScopes = "HmacTimestamp.Scopes"
)

// Defines values for EvaluateRequestSchemaVersion.
const (
	V1 EvaluateRequestSchemaVersion = "v1"
)

// Defines values for EvaluateResponseDecision.
const (
	FAIL EvaluateResponseDecision = "FAIL"
	HOLD EvaluateResponseDecision = "HOLD"
	PASS EvaluateResponseDecision = "PASS"
)

// EvaluateRequest The context a committee verifier sends for one message. It carries the decoded CCIP message and its source-chain provenance so the endpoint does not have to fetch or decode anything itself.
type EvaluateRequest struct {
	// BlockDepth How far the message's block sits below finalized_block_number. Zero when the message's block is the finalized head or newer, which is the case for a message that met its finality requirement against the safe head rather than the finalized one.
	BlockDepth uint64 `json:"block_depth"`

	// FinalizedBlockNumber The source chain's finalized head at the moment the message met its finality requirement. It is fixed at that point, so a message that is retried reports the head as of the original decision rather than the current one.
	FinalizedBlockNumber uint64 `json:"finalized_block_number"`

	// Message The decoded CCIP message. Byte fields are hex-encoded with an 0x prefix; an empty byte field is "0x".
	//
	// Chain selectors are decimal strings, not JSON numbers. They are opaque 64-bit identifiers drawn from the whole uint64 range, and a JSON number is a float64 in JavaScript and in most schema-generated clients, which is exact only to 2^53. Parsing one as a number silently loses precision - about a quarter of the registered selectors are above 2^63 alone. Parse them as strings, or as a 64-bit unsigned integer if your language has one. Counters bounded by a real chain (block numbers, sequence numbers, gas limits) are JSON numbers, because they cannot reach 2^53.
	Message Message `json:"message"`

	// MessageId The CCIP message ID, 32 bytes hex-encoded with an 0x prefix.
	MessageId string `json:"message_id"`

	// SchemaVersion The contract version of this request. Always "v1" for this release; an endpoint serving several verifier releases branches on it.
	SchemaVersion EvaluateRequestSchemaVersion `json:"schema_version"`

	// SourceBlockNumber The source-chain block the message was emitted in.
	SourceBlockNumber uint64 `json:"source_block_number"`

	// SourceTxHash Identifier of the source-chain transaction that emitted the message. It is the raw identifier bytes the source chain reported, hex-encoded with an 0x prefix, whatever the chain family's own convention is - a 32-byte EVM transaction hash and a 64-byte Solana signature both arrive here as hex, not as the chain's native rendering. An endpoint that wants to show or query the native form (base58 for Solana, for example) re-encodes these bytes itself, using source_chain_selector to know which family it is looking at.
	SourceTxHash string `json:"source_tx_hash"`

	// VerifierId Identifies the committee verifier node making the call.
	VerifierId string `json:"verifier_id"`
}

// EvaluateRequestSchemaVersion The contract version of this request. Always "v1" for this release; an endpoint serving several verifier releases branches on it.
type EvaluateRequestSchemaVersion string

// EvaluateResponse The verdict. Returned with HTTP 200; any other status is read as "verdict unknown" and the verifier retries.
type EvaluateResponse struct {
	// Decision PASS signs and attests the message. FAIL drops it — it is never attested and never auto-executed, and recovery needs an operator to replay it, by rescheduling the archived job with the verifier CLI or by rewinding the verifier checkpoint. PASS is matched exactly; FAIL is matched case-insensitively, because reading a verdict as unusable only causes a retry while reading one as PASS causes a signature.
	//
	// HOLD is reserved and not implemented by this release. It is listed here so that implementing it later is an additive change rather than a breaking one for an endpoint that validates responses strictly against this enum. Do not return it: a verifier reads it as "verdict unknown" and retries the message until the task queue's deadline. To hold a message for review today, answer FAIL and replay the message once the review clears.
	Decision EvaluateResponseDecision `json:"decision"`

	// MessageId Echo of the request's message_id. Recommended, but optional and not in the required list, because it is a safety net rather than part of the verdict and requiring it would fail an otherwise correct endpoint that omits it. Nothing else in this response ties the verdict to the message it answers, so echoing the ID lets the verifier refuse a verdict that reached it for a different message, which is what a shared cache or a proxy in front of the endpoint can produce. A mismatch is an error and retries; omitting the field skips the check. Compared case-insensitively.
	MessageId *string `json:"message_id,omitempty"`

	// Reason Optional explanation of a FAIL. The verifier logs it and never signs it. Values longer than 256 characters are truncated in logs.
	Reason *string `json:"reason,omitempty"`
}

// EvaluateResponseDecision PASS signs and attests the message. FAIL drops it — it is never attested and never auto-executed, and recovery needs an operator to replay it, by rescheduling the archived job with the verifier CLI or by rewinding the verifier checkpoint. PASS is matched exactly; FAIL is matched case-insensitively, because reading a verdict as unusable only causes a retry while reading one as PASS causes a signature.
//
// HOLD is reserved and not implemented by this release. It is listed here so that implementing it later is an additive change rather than a breaking one for an endpoint that validates responses strictly against this enum. Do not return it: a verifier reads it as "verdict unknown" and retries the message until the task queue's deadline. To hold a message for review today, answer FAIL and replay the message once the review clears.
type EvaluateResponseDecision string

// Message The decoded CCIP message. Byte fields are hex-encoded with an 0x prefix; an empty byte field is "0x".
//
// Chain selectors are decimal strings, not JSON numbers. They are opaque 64-bit identifiers drawn from the whole uint64 range, and a JSON number is a float64 in JavaScript and in most schema-generated clients, which is exact only to 2^53. Parsing one as a number silently loses precision - about a quarter of the registered selectors are above 2^63 alone. Parse them as strings, or as a 64-bit unsigned integer if your language has one. Counters bounded by a real chain (block numbers, sequence numbers, gas limits) are JSON numbers, because they cannot reach 2^53.
type Message struct {
	// CcipReceiveGasLimit Gas limit reserved for the receiver's ccipReceive call on the destination chain.
	CcipReceiveGasLimit uint32 `json:"ccip_receive_gas_limit"`

	// CcvAndExecutorHash Commits to the CCVs and executor the message requested, 32 bytes hex-encoded.
	CcvAndExecutorHash string `json:"ccv_and_executor_hash"`

	// Data The message payload.
	Data string `json:"data"`

	// DestBlob The destination-chain execution blob.
	DestBlob string `json:"dest_blob"`

	// DestChainSelector CCIP selector of the destination chain, as a decimal string. See the schema description for why this is not a JSON number.
	DestChainSelector string `json:"dest_chain_selector"`

	// ExecutionGasLimit Gas limit reserved for execution on the destination chain.
	ExecutionGasLimit uint32 `json:"execution_gas_limit"`

	// Finality The encoded finality requirement of the message. The low 16 bits are a block-confirmation depth; the high bits are flags.
	Finality uint32 `json:"finality"`

	// OffRampAddress Destination-chain offRamp that will deliver the message.
	OffRampAddress string `json:"off_ramp_address"`

	// OnRampAddress Source-chain onRamp that emitted the message.
	OnRampAddress string `json:"on_ramp_address"`

	// Receiver Destination-chain account that will receive the message.
	Receiver string `json:"receiver"`

	// Sender Source-chain account that sent the message.
	Sender string `json:"sender"`

	// SequenceNumber Per-lane sequence number of the message.
	SequenceNumber uint64 `json:"sequence_number"`

	// SourceChainSelector CCIP selector of the source chain, as a decimal string. See the schema description for why this is not a JSON number.
	SourceChainSelector string `json:"source_chain_selector"`

	// TokenTransfer The token transfer attached to a message. Absent for a message that carries no tokens.
	TokenTransfer *TokenTransfer `json:"token_transfer,omitempty"`

	// Version CCIP message format version.
	Version uint8 `json:"version"`
}

// TokenTransfer The token transfer attached to a message. Absent for a message that carries no tokens.
type TokenTransfer struct {
	// Amount Transferred amount in the token's smallest unit, as a decimal string because it does not fit a JSON number.
	Amount string `json:"amount"`

	// DestTokenAddress Destination-chain token contract.
	DestTokenAddress string `json:"dest_token_address"`

	// ExtraData Pool-specific data carried with the transfer.
	ExtraData string `json:"extra_data"`

	// SourcePoolAddress Source-chain token pool the tokens were locked or burned in.
	SourcePoolAddress string `json:"source_pool_address"`

	// SourceTokenAddress Source-chain token contract.
	SourceTokenAddress string `json:"source_token_address"`

	// TokenReceiver Destination-chain account receiving the tokens.
	TokenReceiver string `json:"token_receiver"`

	// Version Token transfer format version.
	Version uint8 `json:"version"`
}

// PolicyEvaluateJSONRequestBody defines body for PolicyEvaluate for application/json ContentType.
type PolicyEvaluateJSONRequestBody = EvaluateRequest

// RequestEditorFn  is the function signature for the RequestEditor callback function
type RequestEditorFn func(ctx context.Context, req *http.Request) error

// Doer performs HTTP requests.
//
// The standard http.Client implements this interface.
type HttpRequestDoer interface {
	Do(req *http.Request) (*http.Response, error)
}

// Client which conforms to the OpenAPI3 specification for this service.
type Client struct {
	// The endpoint of the server conforming to this interface, with scheme,
	// https://api.deepmap.com for example. This can contain a path relative
	// to the server, such as https://api.deepmap.com/dev-test, and all the
	// paths in the swagger spec will be appended to the server.
	Server string

	// Doer for performing requests, typically a *http.Client with any
	// customized settings, such as certificate chains.
	Client HttpRequestDoer

	// A list of callbacks for modifying requests which are generated before sending over
	// the network.
	RequestEditors []RequestEditorFn
}

// ClientOption allows setting custom parameters during construction
type ClientOption func(*Client) error

// Creates a new Client, with reasonable defaults
func NewClient(server string, opts ...ClientOption) (*Client, error) {
	// create a client with sane default values
	client := Client{
		Server: server,
	}
	// mutate client and add all optional params
	for _, o := range opts {
		if err := o(&client); err != nil {
			return nil, err
		}
	}
	// ensure the server URL always has a trailing slash
	if !strings.HasSuffix(client.Server, "/") {
		client.Server += "/"
	}
	// create httpClient, if not already present
	if client.Client == nil {
		client.Client = &http.Client{}
	}
	return &client, nil
}

// WithHTTPClient allows overriding the default Doer, which is
// automatically created using http.Client. This is useful for tests.
func WithHTTPClient(doer HttpRequestDoer) ClientOption {
	return func(c *Client) error {
		c.Client = doer
		return nil
	}
}

// WithRequestEditorFn allows setting up a callback function, which will be
// called right before sending the request. This can be used to mutate the request.
func WithRequestEditorFn(fn RequestEditorFn) ClientOption {
	return func(c *Client) error {
		c.RequestEditors = append(c.RequestEditors, fn)
		return nil
	}
}

// The interface specification for the client above.
type ClientInterface interface {
	// PolicyEvaluateWithBody request with any body
	PolicyEvaluateWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error)

	PolicyEvaluate(ctx context.Context, body PolicyEvaluateJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error)
}

func (c *Client) PolicyEvaluateWithBody(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPolicyEvaluateRequestWithBody(c.Server, contentType, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

func (c *Client) PolicyEvaluate(ctx context.Context, body PolicyEvaluateJSONRequestBody, reqEditors ...RequestEditorFn) (*http.Response, error) {
	req, err := NewPolicyEvaluateRequest(c.Server, body)
	if err != nil {
		return nil, err
	}
	req = req.WithContext(ctx)
	if err := c.applyEditors(ctx, req, reqEditors); err != nil {
		return nil, err
	}
	return c.Client.Do(req)
}

// NewPolicyEvaluateRequest calls the generic PolicyEvaluate builder with application/json body
func NewPolicyEvaluateRequest(server string, body PolicyEvaluateJSONRequestBody) (*http.Request, error) {
	var bodyReader io.Reader
	buf, err := json.Marshal(body)
	if err != nil {
		return nil, err
	}
	bodyReader = bytes.NewReader(buf)
	return NewPolicyEvaluateRequestWithBody(server, "application/json", bodyReader)
}

// NewPolicyEvaluateRequestWithBody generates requests for PolicyEvaluate with any type of body
func NewPolicyEvaluateRequestWithBody(server string, contentType string, body io.Reader) (*http.Request, error) {
	var err error

	serverURL, err := url.Parse(server)
	if err != nil {
		return nil, err
	}

	operationPath := fmt.Sprintf("/v1/evaluate")
	if operationPath[0] == '/' {
		operationPath = "." + operationPath
	}

	queryURL, err := serverURL.Parse(operationPath)
	if err != nil {
		return nil, err
	}

	req, err := http.NewRequest("POST", queryURL.String(), body)
	if err != nil {
		return nil, err
	}

	req.Header.Add("Content-Type", contentType)

	return req, nil
}

func (c *Client) applyEditors(ctx context.Context, req *http.Request, additionalEditors []RequestEditorFn) error {
	for _, r := range c.RequestEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	for _, r := range additionalEditors {
		if err := r(ctx, req); err != nil {
			return err
		}
	}
	return nil
}

// ClientWithResponses builds on ClientInterface to offer response payloads
type ClientWithResponses struct {
	ClientInterface
}

// NewClientWithResponses creates a new ClientWithResponses, which wraps
// Client with return type handling
func NewClientWithResponses(server string, opts ...ClientOption) (*ClientWithResponses, error) {
	client, err := NewClient(server, opts...)
	if err != nil {
		return nil, err
	}
	return &ClientWithResponses{client}, nil
}

// WithBaseURL overrides the baseURL.
func WithBaseURL(baseURL string) ClientOption {
	return func(c *Client) error {
		newBaseURL, err := url.Parse(baseURL)
		if err != nil {
			return err
		}
		c.Server = newBaseURL.String()
		return nil
	}
}

// ClientWithResponsesInterface is the interface specification for the client with responses above.
type ClientWithResponsesInterface interface {
	// PolicyEvaluateWithBodyWithResponse request with any body
	PolicyEvaluateWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PolicyEvaluateResponse, error)

	PolicyEvaluateWithResponse(ctx context.Context, body PolicyEvaluateJSONRequestBody, reqEditors ...RequestEditorFn) (*PolicyEvaluateResponse, error)
}

type PolicyEvaluateResponse struct {
	Body         []byte
	HTTPResponse *http.Response
	JSON200      *EvaluateResponse
}

// Status returns HTTPResponse.Status
func (r PolicyEvaluateResponse) Status() string {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.Status
	}
	return http.StatusText(0)
}

// StatusCode returns HTTPResponse.StatusCode
func (r PolicyEvaluateResponse) StatusCode() int {
	if r.HTTPResponse != nil {
		return r.HTTPResponse.StatusCode
	}
	return 0
}

// PolicyEvaluateWithBodyWithResponse request with arbitrary body returning *PolicyEvaluateResponse
func (c *ClientWithResponses) PolicyEvaluateWithBodyWithResponse(ctx context.Context, contentType string, body io.Reader, reqEditors ...RequestEditorFn) (*PolicyEvaluateResponse, error) {
	rsp, err := c.PolicyEvaluateWithBody(ctx, contentType, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePolicyEvaluateResponse(rsp)
}

func (c *ClientWithResponses) PolicyEvaluateWithResponse(ctx context.Context, body PolicyEvaluateJSONRequestBody, reqEditors ...RequestEditorFn) (*PolicyEvaluateResponse, error) {
	rsp, err := c.PolicyEvaluate(ctx, body, reqEditors...)
	if err != nil {
		return nil, err
	}
	return ParsePolicyEvaluateResponse(rsp)
}

// ParsePolicyEvaluateResponse parses an HTTP response from a PolicyEvaluateWithResponse call
func ParsePolicyEvaluateResponse(rsp *http.Response) (*PolicyEvaluateResponse, error) {
	bodyBytes, err := io.ReadAll(rsp.Body)
	defer func() { _ = rsp.Body.Close() }()
	if err != nil {
		return nil, err
	}

	response := &PolicyEvaluateResponse{
		Body:         bodyBytes,
		HTTPResponse: rsp,
	}

	switch {
	case strings.Contains(rsp.Header.Get("Content-Type"), "json") && rsp.StatusCode == 200:
		var dest EvaluateResponse
		if err := json.Unmarshal(bodyBytes, &dest); err != nil {
			return nil, err
		}
		response.JSON200 = &dest

	}

	return response, nil
}
