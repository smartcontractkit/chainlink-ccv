// Package verifier holds the verifier's published API contracts and the generators that turn
// them into documentation. It carries no code of its own; the verifier's implementation lives
// under verifier/pkg.
package verifier

//go:generate sh -c "./generate-docs.sh"
