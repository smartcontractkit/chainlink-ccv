# Code generated by tools/configdoc. DO NOT EDIT.
# Verifier secrets reference. Values shown are defaults or illustrative examples.

[db]
  # url is the verifier's application storage database connection URL (CL_DATABASE_URL).
  url = "postgres://user:password@localhost:5432/verifier?sslmode=disable"

[[aggregators]]
  # secret_name is the join key back to the committee verifier config's aggregators.secret_name; omit for the legacy single-aggregator default.
  secret_name = "aggregator_1"
  # api_key is this aggregator's inbound HMAC API key.
  api_key = "<api-key>"
  # secret_key is this aggregator's inbound HMAC secret key.
  secret_key = "<secret-key>"

