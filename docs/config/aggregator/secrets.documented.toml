# Code generated by tools/configdoc. DO NOT EDIT.
# Aggregator secrets reference. Values shown are defaults or illustrative examples.

[storage]
  # url is the application storage database connection URL.
  url = "postgres://user:password@localhost:5432/aggregator?sslmode=disable"

[redis]
  # password is the redis password used by the rate limiter.
  password = "your-redis-password"

[[clients]]
  # client_id identifies the client these credentials belong to; joins to clients.clientId in the config.
  client_id = "client-1"
  # api_key is the client's inbound HMAC API key.
  api_key = "<api-key>"
  # secret_key is the client's inbound HMAC secret key.
  secret_key = "<secret-key>"

