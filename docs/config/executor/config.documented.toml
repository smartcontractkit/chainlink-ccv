# Code generated by tools/configdoc. DO NOT EDIT.
# Executor configuration reference. Values shown are defaults or illustrative examples.

# indexer_address is the list of indexer URLs to receive messages + verifications from.
# The executor will use these for failover if one indexer becomes unavailable.
indexer_address = ["http://indexer-1:8080", "http://indexer-2:8080"]

# source_backoff_duration is the duration to back off after a failed request to the Indexer.
# Defaults to 15 seconds.
# It controls Indexer retries only; NTP uses a fixed 2-second backoff base.
source_backoff_duration = "15s"

# startup_lookback_window is the window of time to look back for new messages when an Executor first starts up.
# Defaults to 1 hour.
startup_lookback_window = "1h0m0s"

# indexer_query_limit is the maximum number of messages to query from the Indexer at once.
# Defaults to 100.
indexer_query_limit = 100

# pyroscope_url is the URL of the Pyroscope server to send metrics to.
pyroscope_url = ""

# executor_id is the ID of this executor. This executorID should be present in the executor pool.
executor_id = "executor-1"

# reader_cache_expiry is the duration for the curse checker cache (RMN cursed state per chain).
# Defaults to 5 minutes.
reader_cache_expiry = "5m0s"

# max_retry_duration is the maximum duration the executor cluster will retry a message before giving up.
# Defaults to 8 hours.
max_retry_duration = "8h0m0s"

# data_not_ready_retry_interval is the base delay between retries when verifier data is not yet available.
# Defaults to 1 second. Exponential backoff applies up to the executor stagger interval.
data_not_ready_retry_interval = "1s"

# ntp_server is the NTP server to use for time synchronization.
# Defaults to time.google.com
# NTP retries are independent of source_backoff_duration.
ntp_server = "time.google.com"

# worker_count is the number of concurrent workers processing messages.
# Defaults to 100.
worker_count = 100

# http_listen_port is the port the executor's own HTTP server (/health) listens on.
# Defaults to 8101. Inert in CL mode, which serves its own API.
# Omitted from marshaled job specs when unset so CL-mode specs are unchanged.
http_listen_port = 8101

# Monitoring is DEPRECATED. Monitoring config is operator-provided and now sourced from the bootstrap
# config (bootstrap.Config.Monitoring), not the JD-shipped app config. This field is retained only so
# that pre-existing JD job specs still carrying a [Monitoring] section continue to decode (app config
# decoding is strict); the executor reads it only as a fallback when the bootstrap config does not
# configure monitoring. Remove once all deployments source monitoring from the bootstrap config.
[Monitoring]
  # LogLevel specifies the logging level for service logger
  LogLevel = ""
  # Pyroscope is the configuration for pyroscope performance monitoring tool
  [Monitoring.Pyroscope]
    # Enabled enables the pyroscope telemetry.
    Enabled = false
    # URL specifies the remote endpoint of pyroscope service
    URL = ""
  # Beholder is the configuration for the beholder client.
  [Monitoring.Beholder]
    # Enabled enables the beholder telemetry.
    Enabled = false
    # InsecureConnection disables TLS for the beholder client.
    InsecureConnection = false
    # CACertFile is the path to the CA certificate file for the beholder client.
    CACertFile = ""
    # OtelExporterGRPCEndpoint is the endpoint for the beholder client to export to the collector.
    OtelExporterGRPCEndpoint = ""
    # OtelExporterHTTPEndpoint is the endpoint for the beholder client to export to the collector.
    OtelExporterHTTPEndpoint = ""
    # LogStreamingEnabled enables log streaming to the collector.
    LogStreamingEnabled = false
    # LogStreamingLevel specifies the level above which logs are streamed to beholder
    LogStreamingLevel = ""
    # MetricReaderInterval is the interval to scrape metrics (in seconds).
    MetricReaderInterval = 0
    # TraceSampleRatio is the ratio of traces to sample.
    TraceSampleRatio = 0.0
    # TraceBatchTimeout is the timeout for a batch of traces.
    TraceBatchTimeout = 0

# chain_configuration is a map of chain selector to chain configuration.
# This is used to configure the chain-specific configuration for each chain such as addresses, executor pool, and execution interval.
[chain_configuration]
  [chain_configuration.1]
    # off_ramp_address is the address of the OffRamp contract on the destination chain.
    off_ramp_address = "0x00000000000000000000000000000000000000ff"
    # transmitter_key_name is the family-specific keystore key name used to sign and submit
    # transactions to the OffRamp on this chain. If empty, accessors fall back to their
    # family's default transmitter key (defined by each family's transmitter package).
    transmitter_key_name = ""
    # executor_pool is the list of executor IDs used for turn taking. This executor's ID must be in the list.
    executor_pool = ["executor-1", "executor-2"]
    # execution_interval is how long each executor has to process a message before the next executor in the cluster takes over.
    execution_interval = "1m0s"
    # default_executor_address is the address of the default executor to check against the message receipts.
    default_executor_address = "0x00000000000000000000000000000000000000ec"

